pipeline {
	agent any

	triggers {
		pollSCM('* * * * *')
	}

	stages {
		stage('Build') {
			steps {
				echo '========================1. Build ====================='
			}
		}
		stage('Deploy'){
			parallel{
				stage('Deploy App'){
					steps{
						echo '====================2 Deploy ========================='
						sh "cp -R /sorc001/applications/ciserv/jenkins/workspace/HongsExchange /sorc001/applications"

					}
				}

			}
		}
	}
}